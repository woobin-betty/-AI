### 프로젝트 기획안

## 프로젝트 개요 (The "Vibe" in Spec)

프로젝트 명: 과제알림서비스

핵심 컨셉: 과제, 업무, 학습 등 다양한 마감 일정을 자동으로 정리하고, 사용자가 놓치지 않도록 알림으로 도와주는 ‘스마트’, ‘정돈’, ‘집중’ Vibe의 일정 관리 서비스.

핵심 목표:
사용자가 해야 할 일과 마감 기한을 입력하면, 시스템이 중요도·마감일·소요시간을 분석하여 우선순위를 자동으로 정리하고, 적절한 시점에 알림을 제공함으로써 효율적인 일정 관리와 스트레스 없는 마감 수행을 돕는다.

타겟 페르소나:

대학생 및 직장인 등 과제·업무 마감이 많은 사용자.

기존 일정 앱(구글 캘린더, 투두이스트 등)을 사용해봤지만, 자동 정렬 및 알림 최적화 기능 부족에 불편을 느낀 사용자.

복잡한 UI, 과도한 기능보다는 직관적이고 깔끔한 인터페이스를 선호하는 사용자.

경쟁보다는 개인 효율과 성취감을 중시하는 사용자.

## 상세 기능 명세
P0 (Core Features - MVP)
1. 데일리 챌린지 시스템 (하루 1개 미션 자동 배포)

[User Story]
사용자는 매일 앱을 켜면 자동으로 생성된 ‘오늘의 챌린지’를 확인하고 수행할 수 있다.

[Spec]

서버는 매일 KST 기준 00:00에 새로운 챌린지를 생성 및 활성화 (is_active = true).

클라이언트는 앱 실행 시 GET /api/challenges/today 호출 → 해당 날짜의 챌린지 데이터 수신.

챌린지 카드에는 다음 정보 표시:

제목(title), 설명(description), 포인트(points), 수행 시간대(time_range)

홈 화면에는 “오늘의 챌린지 시작하기” 버튼만 강조 표시.

하루에 한 번만 수행 가능, 재도전 불가.

2. 챌린지 수행 및 완료 로직 (Core Flow)

[User Story]
사용자는 오늘의 챌린지를 수행하고, 완료 시 보상 포인트를 즉시 획득할 수 있다.

[Spec]

수행 중 상태(in_progress)와 완료 상태(completed)는 클라이언트에서 실시간 반영.

챌린지 완료 시 POST /api/challenges/{id}/complete 호출 → 서버에 결과 저장.

완료 후 축하 애니메이션 및 포인트 획득 모달 표시.

서버는 사용자별 일일 완료 여부(completed_at)를 기록하여 중복 수행 방지.

3. 연속 수행 기록 (Streaks System)

[User Story]
사용자는 며칠 연속으로 챌린지를 수행했는지를 ‘불타는 스트릭 🔥’ 형태로 시각적으로 확인할 수 있다.

[Spec]

서버는 챌린지 완료 시 날짜(completed_date)를 기반으로 streak 계산.

GET /api/user/streaks 호출 시

current_streak (현재 연속 일수),

best_streak (최고 기록) 반환.

메인 프로필 화면에는 불꽃 이모지 애니메이션과 함께 streak 수 표시.

하루라도 수행 안 하면 streak 자동 초기화.

4. 리워드 & 포인트 시스템 (Motivation Loop)

[User Story]
사용자는 챌린지 완료 시 포인트를 획득하고, 누적 포인트로 랭킹 혹은 보상을 받을 수 있다.

[Spec]

각 챌린지는 난이도별 기본 포인트를 가짐 (예: 쉬움 10p, 중간 20p, 어려움 40p).

POST /api/rewards/claim 호출 시 포인트 적립 처리.

사용자 정보(GET /api/user/profile)에는 total_points, level 포함.

홈 화면 상단에 “오늘 획득한 포인트 + 총 누적 포인트” 표시.

P1 (Future Expansion)

소셜 기능 (Community & Ranking):

친구 추가, 주간 랭킹 공개, 챌린지 인증샷 공유 기능.

AI 추천 시스템:

사용자의 수행 패턴과 관심사에 따라 다음 챌린지를 자동 추천.

리마인더 알림 (Push Notification):

사용자가 챌린지를 놓치지 않도록 매일 특정 시간에 “오늘의 미션 도전하자!” 알림 발송.

캘린더 뷰:

한 달 동안 수행한 챌린지를 달력 형태로 시각화.

## UI/UX 요구사항
UX/UI Vibe 원칙: "정돈 (Clarity)"

개발 및 디자인 시 다음 원칙을 최우선으로 고려합니다.

1. 단순함 (Simplicity):

Color Palette: 과제 및 일정 관리를 연상시키는 ‘화이트 / 라이트 그레이 / 네이비 블루’ 중심의 차분한 톤 사용.

Accent Color: 마감 임박 알림, 중요 일정 강조 등에만 제한적으로 ‘블루(Blue)’ 또는 ‘오렌지(Orange)’ 사용.

UI Elements: 복잡한 버튼, 장식적 아이콘, 불필요한 그래픽 효과를 배제하고 명확한 정보 전달 중심으로 구성.

2. 가독성 (Readability):

Fonts: 일정 제목, 과제명 등은 명확하게 인식될 수 있도록 산세리프(Sans-serif) 계열 폰트 사용.

Hierarchy: 오늘 해야 할 일, 임박한 일정, 완료한 일정을 시각적으로 명확히 구분하여 사용자 집중도를 높임.

Layout: 한눈에 ‘오늘의 일정’을 확인할 수 있는 메인 뷰와, ‘주간·월간 관리’가 가능한 보조 탭으로 구성.

3. 방해 요소 최소화 (Non-distraction):

Notification: 과제 마감 알림은 필요 시점에만 제공, 과도한 알림은 지양.

Sound: 알림음은 짧고 부드러운 톤으로 제한하며, 사용자가 직접 음소거 설정 가능.

Animation: 화면 전환 및 알림 등장 시 자연스러운 페이드(Fade) 효과만 사용. 과도한 모션은 배제.
